{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-navigation">
        {% if step > 1 and step < 4 %}
            <a href="{{ path('app_property_new', {step: step - 1}) }}" class="btn btn-secondary">Previous</a>
        {% endif %}

        <button type="submit" class="btn btn-primary">Next</button>
    </div>
{{ form_end(form) }}
